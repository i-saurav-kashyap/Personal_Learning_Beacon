# Beacon environment variables
# Copy this file to `.env.local` and fill in real values, then restart the dev server.

# --- AI Tutor -------------------------------------------------------------
# Anthropic Claude API key. Get one at https://console.anthropic.com/
# Without it, the AI Tutor still loads and explains how to connect — the rest
# of the platform works fully offline.
ANTHROPIC_API_KEY=
